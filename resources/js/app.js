// Importar bootstrap desde node_modules
import 'bootstrap';

// Importamos Alpine.js (si lo necesitas para otros comportamientos en el frontend)
import Alpine from 'alpinejs';

// Asignamos Alpine al objeto window para que sea accesible globalmente
window.Alpine = Alpine;

// Inicializamos Alpine.js
Alpine.start();
